# Лабораторная 1 - Ветеринарная клиника

**Мась Андрей Алексеевич, группа 6411-100503D, вариант 12.**

В этой работе реализованы классы и хранение тестовых данных в памяти.

## Запуск

Требуется .NET SDK 10.0. Команды выполняются из корня репозитория:

```sh
dotnet restore VeterinaryClinic.slnx
dotnet test --solution VeterinaryClinic.slnx --configuration Release --no-restore
```

Тесты используют xUnit.net v3 (`xunit.v3`) и автоматически запускаются в GitHub Actions при push и pull request.

## Модель данных

- `Owner` - ФИО, адрес, телефон владельца; один владелец может иметь несколько питомцев.
- `Pet` - кличка, порода, дата рождения, вес и владелец. Вид животного вычисляется по породе, поэтому значения не могут противоречить друг другу.
- `Breed` - справочник пород с привязкой к `AnimalSpecies`, перечислению видов животных.
- `Veterinarian` - паспорт, ФИО, год рождения, стаж и специализация.
- `Specialization` - справочник специализаций с указанием вида животного.
- `Appointment` - запись питомца к врачу: местные дата и время клиники, кабинет и признак повторного приёма. Это доменный контракт, указанный в задании.

Сущности имеют идентификаторы. Связи в первой лабораторной представлены ссылками на объекты; каждый тест получает новый набор данных. Все персональные данные в нём вымышлены.

Датасид содержит 10 пород, 10 владельцев, 13 питомцев, 10 специализаций, 10 врачей и 17 приёмов. Всего выполняется 17 тестовых случаев.
